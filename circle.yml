machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t nogsantos/postgresql .
    - docker run -d --name=postgresql nogsantos/postgresql; sleep 10
    - docker run -it --volumes-from=postgresql nogsantos/postgresql sudo -u postgres -H psql -c "\conninfo"
